active: true
derived: false
group: LIFE
header: |
  LifecyclePolicy
level: 1.15
links:
- ARCH007: MkJlOhLdiJGrZjzqUdBCh16vBvG0YeuWx1qsw0ETY6w=
normative: true
ref: ''
reviewed: Zt2-20d7MMvVjuJ5RGV49P0JvzMqaEIwwXkg9IF19c0=
text: |
  ## インターフェース

  ```python
  class LifecyclePolicy:
      def add_rule(self, pattern: str, retention: Any) -> None: ...
      def resolve_with_fallback(self, func_name: str) -> float | None: ...
  ```

  ## 振る舞い

  1. **順次照合**: 登録された順にルールを走査する
  2. **パターンマッチ**: `fnmatch.fnmatch` を使用し、関数名がパターンに合致するか判定
  3. **二段階解決**:
     - 最初に「完全修飾名 (module.qualname)」で照合
     - マッチしなければ「短縮名 (qualname)」で照合
  4. **結果返却**: 最初にマッチしたルールの保持期間を秒数に変換して返す

  ## パラメータ詳細

  | パラメータ | 例 | 説明 |
  |---|---|---|
  | `pattern` | `"my_mod.*"`, `"*_test"` | Glob形式のパターン |
  | `retention` | `"30d"`, `3600` | 保持期間の定義 |

  ## エラーハンドリング

  - **マッチなし**: 全てのルールにマッチしない場合は、`None`（無期限保持）を返す。

  ## エッジケース

  - **重複パターン**: 先に登録されたルールが優先される。
