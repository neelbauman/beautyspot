active: true
derived: false
gherkin: |
  Feature: Recursive argument canonicalization
    Scenario: Normalizing various Python types for stable hashing
      Given an object of any supported type
      When canonicalize is called on the object
      Then it recursively converts the object into a stable, sortable primitive representation
groups:
- KEY
header: |
  引数の再帰的正規化
level: 2.5
links:
- ARCH002: HVEL9BgCJqagLk2CA_ksXF9TleinSEfxiUcowkxwoJs=
normative: true
ref: ''
reviewed: O-Ed-ZYg2nZC0bofNauyQsz8eB-Yi3sfrSvNcy8ks18=
text: |
  ## インターフェース

  ```python
  @singledispatch
  def canonicalize(obj: Any) -> Any: ...
  ```

  ## 振る舞い

  `singledispatch` を使用し、型に応じた最適な正規化形式へ再帰的に変換する。

  ### 主要な型別の処理
  - **基本型 (int, float, str, bytes)**: そのまま返す
  - **bool**: `("__bool__", value)` のタプルに変換（`1` と `True` の衝突を防止）
  - **dict**: キーでソートし、`[[k1, v1], [k2, v2], ...]` のリストに変換
  - **list / tuple / set / frozenset / deque**: それぞれ専用の型タグ（例: `"__list__"`) を付与し、要素を再帰的に正規化。集合型はソートを行う
  - **Enum**: `("__enum__", module, qualname, value)` に変換
  - **カスタムオブジェクト**: `__dict__` または `__slots__` から属性を取得し、型名と共に正規化

  ## パラメータ詳細

  | オブジェクト型 | 正規化後の形式 | 備考 |
  |---|---|---|
  | `numpy.ndarray` | `("__numpy__", shape, dtype, bytes)` | 高速かつ正確なハッシュを保証 |
  | `OrderedDict` | `("__ordered_dict__", items)` | 挿入順序を保持したまま正規化 |
  | `Pydantic Model` | `("__pydantic_v2__", schema)` | スキーマ情報をベースに判定 |

  ## エラーハンドリング

  - **未対応型**: 警告をログ出力し、`str(obj)` にフォールバックする
  - **循環参照**: `RecursionError` をキャッチし、その階層で `str()` による正規化に切り替える

  ## エッジケース

  - **異なる型の比較**: `_safe_sort_key` により、Python 3 で比較不可能な型同士（例: `int` と `str`）が辞書のキーに含まれていても安定したソート順を保証する

  ## 設計判断

  辞書のキーの順序違いや、リストとタプルの違いなど、意味的には同じだが構造が異なる入力に対して、安定した同一のキャッシュキーを生成し、キャッシュミスを防ぐため。
